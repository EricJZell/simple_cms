<% @page_title = "Delete Page" %>
<%= link_to("<< Back to List", subject_pages_path(@subject), class: 'back-link') %>
<div class="pages delete">
  <h2>Delete Page</h2>

  <%= form_for([@subject, @page], method: :delete) do |f| %>
    <p>Are you sure you want to permanantly delete this page?</p>
    <p class="reference-name"><%= @page.name %></p>
    <div class="form-buttons">
      <%= submit_tag("Delete Page") %>
    </div>
  <% end %>
</div>
